# 设计方案

## 技术需求

- DNS协议处理
- 并发能力
- 命令参数
- 良好的日志系统
- 可配置

## 系统设计

#### 技术栈/开发环境
 linux/mac
 c标准库和posix标准库
 clang/gcc
##### 多线程库

 <threads.h> 

##### 网络库

使用udp协议

###### posix

#include <sys/socket.h>    // socket 相关函数
#include <netinet/in.h>    // sockaddr_in 结构
#include <arpa/inet.h>     // inet_addr, htons 等转换函数
#include <unistd.h>        // close 函数


### 模块划分

模块：函数和类型定义集合体，可以持有自己的运行时数据（static 变量）。相当于面向对象中的单例对象。

#### 基础设施模块

采用分层架构，上层持有下层的结构体，调用下层的api。

##### 容器模块

stl.h

数据容器类型定义、以及增删改查方法

本层不持有数据，只是类的静态定义，具体的实例化对象交由上层持有

##### 异常模块
exception.h

- 提供异常追踪机制，见exception头文件

##### 网络模块
socket.h

- socket封装。

##### 日志模块
logger.h

提供日志打印api.被其他模块依赖，提供日志打印函数，支持信息分级。

##### 配置中心
config.h

读取（持有）本应用的配置信息

- 配置信息在此层读取解析（并通过static变量存储），上层使用api访问数据。
- 通过策略模式将解析流程和解析行为解耦

#### 核心业务模块

##### 缓存模块
cache.h

负责缓存的管理，实现缓存策略

- 持有缓存对象

- 缓存crud接口

整个模块作为一个单例类而存在

##### 协议模块
protocol.h id.h

负责实现dns协议

- dns数据包的序列化、反序列化
- dns包的转换、构造
- id池

##### 服务器模块
server、session、daemon

- 负责请求会话的管理
- 执行包的收取和发送
- 执行周期性检查任务

###### 并发模型设计

因为整个程序只有一个socket,无法并行收包/发包，因此网络IO使用多线程没有意义。

- 主线程通过select事件驱动模式实现并发请求接收和超时管理，避开多线程复杂度的同时优雅解决并发问题。协议数据的处理并不涉及高复杂度算法，因此整个系统的瓶颈只在于毫秒级的网络延迟。

- 缓存过期、配置更新等操作则通过后台守护线程执行。

##### 用户接口模块（main.c）

- 解析启动参数，加载配置
- 模块初始化
### 依赖类型

按照依赖的对象可分为两类。

数据型依赖

——其他模块需要通过配置模块读取配置文件

——协议模块需要通过系统模块读取hosts文件内容

——日志模块需要用户接口层注入配置信息（被动依赖）

接口型依赖

——其他模块依赖容器模块的api来创建、读写容器。

——服务器模块依赖系统模块来创建socket和线程。

——服务器模块依赖协议模块进行数据解析和通信决策

——其他模块需要日志模块的函数来打印日志

## 协议标准

## 编码规范

#### 命名

函数命名使用蛇形命名法: some_function

结构体类型命名：使用大驼峰命名法:  SomeStruct

常量命名使用全大写蛇形：DEFAULT_VALUE

变量命名较为灵活，但不得使用大驼峰或全大写蛇形。

#### 注释

头文件中的函数必须有注释，说明参数、返回值以及副作用。

#### 代码结构

代码必须良好封装，头文件中不得声明变量。

头文件中未声明的函数，源文件中应声明为static.

所有状态在.c文件中维护。

所有使用到容器的地方，应当注释说明元素类型。一个容器实例只能存储一种类型

## to fix
明确的内存所有权模型。高优先级，防止不明错误。